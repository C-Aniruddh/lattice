node_modules/
dist/
coverage/
*.tsbuildinfo
.DS_Store
.vite/
.lattice/reports/
.lattice/doccheck/
dist-ts/

# Written by `npm test` so `npm run measured` can check the recorded counts without a second run.
.lattice/.test-counts.json
