# @lattice/draw

> The rendering layer: a Surface interface with a Canvas2D backend, colour derivation, and the isometric solid kit that makes procedural art read as designed.

Part of **[Lattice](https://github.com/C-Aniruddh/lattice)** — the grid underneath.

```bash
npm i @lattice/draw
```

_This README is written by the agent that builds the package. See `AGENTS.md`._
