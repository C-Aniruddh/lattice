/**
 * @lattice/loop — Time. A wall-clock game loop with fixed-step simulation and interpolated rendering, plus scheduling, tweens and frame statistics.
 *
 * The public surface of this package. Every symbol a consumer may use is re-exported here
 * and nowhere else; `.lattice/kit.json` lists them, and `npm run lint` keeps that list honest.
 */

/** The kit version this package was built as part of. */
export const VERSION = '0.1.0';
