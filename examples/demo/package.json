{
  "name": "@latticekit/demo",
  "version": "0.1.0",
  "private": true,
  "description": "LAMP ROAD — the first gallery exhibit. A valley at dusk, and the proof that the seams fit.",
  "type": "module",
  "scripts": {
    "dev": "vite --port 5173 --strictPort",
    "build": "vite build",
    "preview": "vite preview --port 5174 --strictPort"
  },
  "$comment": "Eight of nine. @latticekit/persist is the one that is absent, and that is a finding rather than an oversight: this exhibit has no save, per docs/GALLERY.md, so nothing in the gallery plan yet depends on it. @latticekit/ui was declared and unimported for a whole iteration because the HUD was drawn into the canvas; it is the overlay now, which is what GALLERY.md made a rule.",
  "dependencies": {
    "@latticekit/audio": "^0.1.0",
    "@latticekit/core": "^0.1.0",
    "@latticekit/draw": "^0.1.0",
    "@latticekit/input": "^0.1.0",
    "@latticekit/iso": "^0.1.0",
    "@latticekit/loop": "^0.1.0",
    "@latticekit/sim": "^0.1.0",
    "@latticekit/ui": "^0.1.0"
  }
}
